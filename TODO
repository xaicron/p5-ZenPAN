TODO: into sub-command

$ zenpan add -i XAICRON MyModule-0.01.tar.gz
$ zenpan add < zenfile

$ zenpan remove MyModule                     # remove all version
$ zenpan remove MyModule~0.01                # remove version 0.01
$ zenpan remove XAICRON/MyModule-0.01.tar.gz

$ zenpan reindex
